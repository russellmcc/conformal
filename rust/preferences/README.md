This crate contains operations for working with persistant preferences systems.

Currently we support the macOS `defaults` system, but we aspire to add support for more preference platforms over time.
