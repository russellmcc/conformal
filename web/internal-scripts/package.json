{
  "name": "conformal-internal-scripts",
  "description": "Build scripts for building the conformal audio framework itself",
  "private": true,
  "homepage": "https://github.com/russellmcc/conformal",
  "bugs": "https://github.com/russellmcc/conformal/issues",
  "license": "ISC",
  "scripts": {
    "lint": "tsc && eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "check-format": "prettier --ignore-path .gitignore -c .",
    "format": "prettier --ignore-path .gitignore --write .",
    "validate": "echo 'dummy validation'"
  },
  "bin": "./src/index.ts",
  "type": "module",
  "dependencies": {
    "@commander-js/extra-typings": "catalog:",
    "commander": "catalog:",
    "tmp-promise": "catalog:",
    "zod": "catalog:"
  }
}
